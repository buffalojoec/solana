# PR #7998 — program-runtime: port over SyscallInvokeSigned

- **Commit:** `8c90cc1548f47b4f8de3952ada3f192494a67ef8`
- **Date:** 2025-09-11
- **Files changed:** 3 (136 insertions, 134 deletions)

## Summary

Moves the `SyscallInvokeSigned` trait and `cpi_common()` function from
`syscalls/src/cpi.rs` to `program-runtime/src/cpi.rs`. These are the
core CPI dispatch entry points.

## File Summary

### Category A: Pure Moves

| Item | Notes |
|------|-------|
| `SyscallInvokeSigned` trait | Visibility: private -> `pub`. Body identical. |
| `cpi_common()` function | Visibility: private -> `pub`. Body identical. |

### Category B: Structural Changes

- **`const SUCCESS: u64 = 0;` added** to `program-runtime/src/cpi.rs`.
  Previously available via `use super::*` which pulled in
  `solana_program_entrypoint::SUCCESS`. Now defined locally with the
  same value.
- **Import moves:** `ExecuteTimings`, `Measure`, `Instruction` moved to
  program-runtime imports. `ExecuteTimings` removed from syscalls imports.

### Category C: Behavioral Changes

**None.** No error variant changes in this PR. `cpi_common` delegates to
other functions that were already moved in prior PRs.

### Category D: Test-Only Changes

None.

## Security Checklist

- [x] Signer privilege checks preserved
- [x] Account ownership validation preserved
- [x] CPI depth/nesting enforcement preserved
- [x] Account data serialization/deserialization unchanged
- [x] Lamport balance invariants maintained
- [x] Program ID validation on CPI targets preserved
- [x] `is_writable` checks preserved
- [x] PDA signer seed validation preserved
- [x] Account realloc bounds preserved
- [x] Duplicate account handling preserved
- [x] No consensus-breaking changes without feature gates
- [x] No non-deterministic operations introduced
- [x] No `unwrap()` / `expect()` introduced in production code paths
- [x] No `debug_assert!` replacing proper error returns
- [x] No visibility escalation exposing internal invariants

## Verdict

**PASS** — Pure move with zero behavioral changes. The `SyscallInvokeSigned`
trait and `cpi_common` function are identical to their originals. No error
type changes in this PR.
